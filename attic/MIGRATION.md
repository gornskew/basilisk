# Migrating the yard out of skewed-emacs

> **MOTHBALLED (2026-08-16).** The migration is done: the yard runs from
> this repo, every host is cut over, and `skewed-emacs/mcp/` is deleted.
> Nothing downstream depends on the old arrangement, so this is history
> rather than instructions — kept for anyone asking *why* a file sits
> where it does, and as the record of one classification that was wrong
> (see the CORRECTION under "Needs splitting, not moving"). Do not follow
> the steps below; they describe a repository state that no longer
> exists.

Status as of **2026-08-15**: nothing has moved. The framework is fully
working in `skewed-emacs/`. This document is the manifest, written before
any file moves so the move itself is mechanical.

Survey basis: `skewed-emacs` at `devo` HEAD `4f677ed`, with all five stack
overlays converted to symbolic rosters and their generated compose YAML
verified byte-identical.

## The cut

The guiding line: **basilisk owns everything needed to bring a fleet up;
skewed-emacs owns the captain's configuration and the image carrying it.**

Notably this includes the *base* `services.sexp`. That is the part worth
being deliberate about — see "Why the base definition comes too" below.

### Moves to `basilisk/`

| path in skewed-emacs | what it is |
|---|---|
| `compose-dev` | the stack driver, ~1000 lines of POSIX sh |
| `basilisk` | symlink → `compose-dev` (the preferred name) |
| `generate-configs.el` | generator: compose YAML, MCP configs, elisp discovery, install scripts, eyes-only heap probes |
| `generate-env.sh` | `.env` generation, branch detection |
| `fittings.sexp` | the fitting catalogue: posts → container services, crew-levels, probes |
| `services.sexp` | the **base** stack definition (SSoT for the vanilla fleet) |
| `docker-compose.yml` | generated from the above |
| `BASILISK.md` | the naming, the crew, the conceit |
| `systemd/`, `systemd-system/` | unit + installer that bring the stack up at boot |

### Stays in `skewed-emacs/`

| path | what it is |
|---|---|
| `dot-files/` | the Emacs configuration itself — the actual product |
| `setup` | host-side installer for that configuration |
| `docker/` | the image build: `Dockerfile`, `build`, `run`, `skewed-install`, `webshot`, `webshot-clip`, `emacs-repl.sh` |
| `img/`, `windows-keybindings` | image assets, keybinding docs |
| `docs/` | mostly Captain-facing; a few files need a pass (below) |

### Needs splitting, not moving

**`mcp/`** — mixed. `mcp-container.json`, `mcp-windows.json`, `mcp.toml`
and the `narad-stack-*` variants are *generated by* `generate-configs.el`
and describe the whole fleet's backends → yard. `merge-mcp-configs.el`
and `mcp-exec` are fleet-level plumbing → yard. `opening-prompt.md` is
Captain-flavored → yard too, in the end, since it primes a session
against the whole roster. Decide per file rather than moving the
directory wholesale.

> **CORRECTION (2026-08-16).** This list originally put `entrypoint.sh`
> in the fleet-level group alongside `mcp-exec`. That was wrong, and it
> broke the build: `entrypoint.sh` starts the *skewed-emacs image's* own
> Emacs daemon, waits for it, optionally starts ttyd, and execs
> `docker/emacs-repl.sh`. It is image-level, not fleet-level — and the
> yard never referenced it, so the copy that came here had no consumer
> at all.
>
> When `mcp/` was finally deleted from skewed-emacs, pipeline 3630 failed
> on `Dockerfile:308  COPY mcp/entrypoint.sh` → `not found`. It now lives
> at `skewed-emacs/docker/entrypoint.sh`, beside the neighbours it always
> belonged with. The lesson worth keeping: "which repo does this concept
> belong to" and "which build reads this file" are different questions,
> and only the second one breaks CI.

**`README.md` and `CLAUDE.md`** — both currently describe all three
assets. Each wants splitting, with the stack half moving here. The
skewed-emacs README's own three-asset section is the natural seam: it
already makes the argument for this repo existing.

**`docs/`** — `CLAUDE_DESKTOP.md`, `HOST_EMACS_MCP.md`,
`FIRST_SESSION_TRANSCRIPT.md` and `DEMO_GIF.md` all reference
`compose-dev`. Re-point rather than move; they are onboarding docs for
the editor, which happens to arrive via the stack.

### Does not move, and should be deleted or ignored

- `narad-stack-compose.yml` — a **copy** (not a symlink) of narad's
  overlay sitting in the working directory because this dev host is
  narad. It is the documented glob footgun: `_collect_compose_files`
  globs `./*.yml`, so a second host overlay in the same directory would
  merge two fleets silently. Do not carry it here.
- `compose-dev.pre-validation-refactor` — stale backup.

## Why the base definition comes too

`fittings.sexp` marks `:captain`, `:ship-engineers` and `:medic` with
`:in-base t`, meaning "already defined by the vanilla
`docker-compose.yml`, so an overlay emits only the delta." That flag is
an assertion about what the base `services.sexp` contains.

Leave the base behind in `skewed-emacs` and `:in-base` becomes an
unenforced cross-repo invariant — precisely the drift the symbolic
rosters were built to eliminate, reintroduced one level up. Bringing the
base along keeps `:in-base` a statement about a file in the same repo.

The cost is that `basilisk` then contains a definition of the
`skewed-emacs` *service*. That is correct and not a leak: it references
the image by name (`gornskew/skewed-emacs:...`), exactly as it references
`gornskew/cyclops:master`. No repo coupling, only an image reference.

## What breaks, and how much

### External references — 13 sites, all cosmetic or one-line

Every stack overlay's `services.sexp` header carries a regeneration
recipe, and three stack READMEs repeat it:

```
sally-stack/services.sexp:16     (load "/projects/skewed-emacs/generate-configs.el")
shelly-stack/services.sexp:17    (load "/projects/skewed-emacs/generate-configs.el")
elsie-stack/services.sexp:18     (load-file "/projects/skewed-emacs/generate-configs.el")
narad-stack/services.sexp:16     (load-file "/projects/skewed-emacs/generate-configs.el")
balaram-stack/services.sexp:24   (load-file "/projects/skewed-emacs/generate-configs.el")
sally-stack/README.md:36         same
shelly-stack/README.md:36        same
balaram-stack/README.md:42       same
```

Plus five prose mentions of `skewed-emacs/fittings.sexp` in the same
`services.sexp` headers, and one cross-link in `cyclops/README.md:18`
pointing at `../skewed-emacs/BASILISK.md`.

All are comments or documentation. None is executed by the generator.
They become `/projects/basilisk/generate-configs.el` and
`../basilisk/BASILISK.md` in a single sweep.

Note that `skewed-gen-fittings-file` in `generate-configs.el` defaults to
`/projects/skewed-emacs/fittings.sexp` — that one **is** executed and
must change with the move.

### `bootstrap_from_image` — the one genuinely hard problem

`compose-dev:58-95` handles the standalone case: someone curls
`compose-dev` on a bare box with no clone. With no `docker-compose.yml`
beside it, it creates a container from the skewed-emacs image and
`docker cp`s three items out of `/home/emacs-user/skewed-emacs/`:

```
docker-compose.yml    generate-env.sh    mcp/
```

**All three are yard files.** So the skewed-emacs *image* is currently a
delivery vehicle for the framework, and the split breaks that path unless
it is addressed. Three options, in preference order:

1. **Vendor basilisk into the skewed-emacs image** at build time (a
   snapshot under `/home/emacs-user/basilisk/`), and re-point the `docker
   cp` source. Smallest change; keeps one-command standalone bootstrap
   working with no second pull. Costs a build-time dependency from the
   image on this repo.
2. **Bootstrap from a basilisk image.** Cleanest conceptually — the yard
   ships the yard — but means publishing an image whose only job is to
   carry text files, and a second pull on first run.
3. **Fetch from gitlab over HTTPS.** No image involvement, but it needs
   network access to `gitlab.genworks.com` and credentials for a private
   repo, which the current path deliberately avoids.

**DECIDED (Dave, 2026-08-15): option (1)** — vendor a basilisk snapshot
into the skewed-emacs image and re-point the `docker cp` source. Revisit
(3) once the github mirror exists.

### The bare one-liner still has to work

Dave, 2026-08-15: "the one-liner `./basilisk` can come from basilisk now
— that should still work bare."

So the standalone entry point inverts. Today you curl `compose-dev` out
of the **skewed-emacs** repo and it bootstraps its runtime files from the
skewed-emacs image. After the move you curl `basilisk` out of **this**
repo, and it still bootstraps from the skewed-emacs image (option 1
above) — the script comes from the yard, the payload rides in the
Captain's image.

That is a coherent split rather than an accident: the image is a
convenient, already-published, already-pulled carrier, and the standalone
user is about to pull it anyway. It does mean the vendored snapshot must
stay fresh, so the image build should copy basilisk in at build time
rather than anyone hand-syncing it.

Acceptance test for this path, worth writing down because it is the one
users hit first and the one nobody runs by accident:

```
mkdir /tmp/bare && cd /tmp/bare
curl -O <basilisk raw URL>/basilisk && chmod +x basilisk
./basilisk up
```

must bring a stack up in an empty directory with no clone of anything.

Also note the error message at `compose-dev:66` points at
`https://github.com/gornskew/skewed-emacs` as the fallback clone
instruction. It will want to name this repo — at its **gitlab** URL,
since the github mirror does not exist yet — once the yard lives here.

## STATUS 2026-08-15: phase 1 done — machinery populated and verified

The yard is now **copied** into this repo and proven to work from here.
Nothing has been removed from `skewed-emacs`, which is still fully
functional and untouched.

**Sequencing (Dave):** *"Once basilisk is populated with ostensibly
working machinery, I think we just rip any stack-related stuff right out
of skewed-emacs and direct the reader of any READMEs to basilisk as the
slipperiest path to getting a skewed-emacs container spun up."*

Copy-then-verify-then-rip, rather than the `git mv` this document
originally proposed. It is the safer order and it is also the only one
that actually works across two separate repositories: at no point is the
stack unbootable, and the removal step becomes trivial once the
replacement is known good.

**Branch:** this repo tracks `devo`, for parity with `skewed-emacs` and
`lisply-mcp`. (`eyes-only` stays on master/main "in raw-dogging
tradition until anybody actually starts buying this stuff".)

### Acceptance test: passed

Regenerated all five host stacks plus the base from **this** repo's
`generate-configs.el`:

| output | result |
|---|---|
| sally / shelly / balaram / elsie / narad overlay compose | **byte-identical** |
| base `docker-compose.yml` | **byte-identical** |
| `skewed-emacs` working tree | untouched, clean |

### Two changes the move required

1. **`skewed-gen-fittings-file` now resolves beside the loaded generator**
   instead of being hardcoded to `/projects/skewed-emacs/fittings.sexp`,
   and is `setq`'d at load time rather than left to `defvar`.

   The `defvar` part is not pedantry — it bit immediately. A `defvar`
   default is evaluated only on the first load in a session and then left
   alone (exactly the property eyes-only's `parameters.lisp` depends on
   to survive hot reloads). With two copies of the generator side by side
   during the move, a session that had loaded skewed-emacs's copy kept
   pointing at **skewed-emacs's catalogue** after loading this one, and
   generated from the wrong repo with nothing in the output to say so.
   Caught by asserting the resolved path rather than trusting it.

2. **Base-name handling.** `basilisk` joins `skewed-emacs` as a name that
   means "this is a base, not a host overlay" — so it generates an
   unprefixed `docker-compose.yml`, and does not get an overlay `install`
   script (a base repo is what you run the stack *from*, not something
   you install into another checkout).

### Entanglements found while doing it, not visible from the survey

- ~~`compose-dev` uses `SCRIPT_DIR="$(pwd)"`, not the script's own
  location.~~ **WRONG — corrected.** Line 13 is `cd "${0%/*}"`, so the
  script changes to its own directory *before* taking `pwd`. `SCRIPT_DIR`
  is therefore the script's own location and `./basilisk` IS runnable
  from anywhere. The bare-one-liner story is unaffected.
- **The generator writes into `dot-files/`.** `services-generated.el` is
  yard *output* but lands in the Captain's configuration tree
  (`dot-files/emacs.d/etc/`), which this document assigns to
  `skewed-emacs`. **Resolved in principle — see the next section.**
- **`compose-dev` also reads `mcp/` and expects `.git` beside it**, which
  is why `mcp/` came across wholesale rather than per-file. The per-file
  split proposed above is still the right end state and is still pending.

## Generated topology moves to `/etc/basilisk/` — the container metadata area

Dave, 2026-08-15: *"services-generated definitely needs to start going
somewhere out of the skewed-emacs tree, to some kind of container
metadata area if we have a pattern or api for that."*

**We have two, and one of them fits exactly.**

### The existing patterns

1. **`/tmp/skewed-crew-identity`** — `compose-dev`'s
   `mint_crew_identities()` writes `NAME=` / `SPECIES=` / `ROLE=` into
   every container over `docker exec`, and `metrics.lisp` reads it in
   preference to minting its own. Live right now in this container:
   `NAME=Drelilk SPECIES=skewed-emacs ROLE=captain`.
   Per-container, minted at boot, pushed in.
2. **`/etc/<product>/<file>`** — read-only bind mounts from host to
   container: `/etc/cyclops/cyclops.sexp`,
   `/etc/cyclops-checkout/production-marker`,
   `/etc/royalties/production-marker`.
   Generated on the host, mounted in, never copied.

Generated stack topology is **pattern 2**. It is produced on the host at
config-generation time, identical for every container in a stack, and
read-only to the consumer. Nothing about it wants a per-container mint.

### The proposal

- **Target inside containers: `/etc/basilisk/`.** Directly analogous to
  `/etc/cyclops/`, and in the yard's own namespace rather than the
  Captain's.
- **Source on the host: a `generated/` directory in the stack's own
  checkout**, holding `services-generated.el` plus any
  `<host>-services-generated.el` overlay.
- **Delivery: a read-only bind mount in the base `services.sexp`
  `:defaults :volumes`,** so every service in every stack gets it without
  each one declaring it.
- **Consumer change:** `services-discovery.el` — which stays with the
  Captain, since it is Emacs configuration — gains `/etc/basilisk/` as
  the FIRST candidate for its generated-dir, keeping its current
  location as a fallback for the transition.

### What that buys, beyond tidiness

- **It resolves the `dot-files/` question outright.** Yard output stops
  landing in the Captain's configuration tree, so `dot-files/` goes back
  to meaning only "the Emacs configuration", and the rip has a clean
  edge to cut along.
- **It deletes a copy step.** `compose-dev:608-616` currently copies
  `*-services-generated.el` and `services-discovery.el` into the running
  container. A mount removes the copy, and with it the staleness window
  where a container runs topology from a previous generation.
- **It is the same shape the Pilot already uses,** so there is one
  convention for "config generated on the host, read in the container"
  rather than two.

Note `/etc` is typically unwritable by the non-root user these images run
as — irrelevant here, and in fact the point: the mount is `:mode "ro"`
and the host owns the content.

### Step one done: generation now also writes `generated/`

`generate-configs.el` writes the services elisp to **both**
`generated/` (new home) and `dot-files/emacs.d/etc/` (old home) during
the transition. Purely additive — all six compose files regenerate
byte-identical, and every existing consumer still works. `generated/`
now exists in basilisk and all five stack checkouts.

### THE MOUNT HAS A HOLE, found before cutting

The obvious next move — declare the `/etc/basilisk/` mount once in the
base `services.sexp` `:defaults :volumes` — **does not work**, and it
fails at the worst possible place.

An overlay compose defines only the services that stack deviates on.
Sally's defines `cyclops` and `gendl-ccl`. The other three —
`skewed-emacs`, `gendl-sbcl`, `autoheal` — come from the **base** compose
alone. Docker compose merges long-syntax volumes by container path, so
the base's mount wins for any service the overlay does not redefine.

Which means on sally, `skewed-emacs` would mount **basilisk's** generated
directory rather than sally's — and `skewed-emacs` is precisely the
container that runs `services-discovery.el`. The Captain would read base
topology and never see sally's Pilot. That is a regression against
today's copy-based mechanism, not an improvement.

Related fact that closes off the easy escapes: overlays are *installed
into* the base checkout (the overlay's generated `install` script copies
its `.yml` and mcp configs across), and `compose-dev` globs `./*.yml`
from that one directory. So "which stack am I" is determined by **which
overlay was installed**, and `generate-env.sh` emits no stack identifier
at all — only `PROJECTS_DIR`.

### Three ways out, in order of preference

**(c) Repoint the existing copy — recommended, and it fully satisfies the
ask.** Leave the mount out entirely. Change `compose-dev:608-616` to copy
from `generated/` instead of `dot-files/emacs.d/etc/`. Yard output leaves
the Captain's tree, `dot-files/` goes back to meaning only "the Emacs
configuration", the rip gets its clean edge — and nothing about compose,
mounts, `.env` or discovery changes at all. Near-zero risk. It keeps the
copy step, so it does not fix the staleness window, but that window is a
refinement rather than the problem Dave named.

**(a) Per-stack env var, later.** `BASILISK_GENERATED` in `.env`, written
per stack, with the base `:defaults` mount reading
`${BASILISK_GENERATED:-${PROJECTS_DIR}/basilisk/generated}`. One mount
definition, correct for base-defined services, removes the copy and the
staleness window. Cost: reintroduces a `.env` dependency that the
`{{STACK}}` decision deliberately avoided — though that decision was
about the *catalogue*, and a per-host path is genuinely `.env`'s job.
Wants `generate-env.sh` to learn which overlay is installed.

**(b) Overlays redeclare every base service to carry the mount.**
Rejected on sight: that is exactly the restatement the symbolic rosters
just deleted.

**Not yet implemented beyond step one.** (c) is a small, contained change
and is the obvious next commit; (a) is the follow-on once a stack has
actually booted against a mount.

### Deferred: the `mcp/` split

`mcp/` came across wholesale and its per-file split is **its own
conversation** (Dave, 2026-08-15), not part of this migration.

## Does `./basilisk up` disturb the running stack? — analysed, not guessed

Short answer: **it will fail, not take over.** But do not run it as-is.

| fact | value |
|---|---|
| running stack's compose project | `skewed-emacs` |
| its working dir | `/home/dcooper8/projects/skewed-emacs` |
| its compose files | base + `narad-stack-compose.yml` |
| `COMPOSE_PROJECT_NAME` set anywhere? | **no** — not in compose-dev, generate-env.sh or any `.env` |
| so project name comes from | the directory basename → **`basilisk`** |
| `container_name:` in base compose | explicit: `skewed-emacs`, `gendl-ccl`, `gendl-sbcl`, `autoheal` |
| network | explicit `name: skewed-network` — shared, not project-scoped |

Because the project name differs, compose sees no existing containers of
its own. It then tries to create a container literally named
`skewed-emacs` — and **container names are global to the daemon, not
scoped to a project.** The result is `Conflict. The container name
"/skewed-emacs" is already in use`, and compose aborts.

So the running stack survives. `run_compose down` lives in
`stop_services()`, not on the `up` path, and would be project-scoped
anyway.

**Three reasons not to try it regardless:**

1. `/projects/basilisk` holds only `docker-compose.yml`. The narad
   overlay is not installed here, so even a successful boot would be the
   *base* fleet — no Pilot, no Guild units. Not narad's actual stack.
2. There is no `.env` here; the run would generate one, mutating the
   checkout as a side effect of a test.
3. The shared `skewed-network` is referenced by explicit name, so both
   projects contend for the same network object.

### The real test is a deliberate cutover, and it drops the MCP link

Not a parallel boot. The sequence is:

1. Install the narad overlay into `/projects/basilisk` (run
   `narad-stack/install` against it) so the file set matches what
   `/projects/skewed-emacs` currently has.
2. `cd /projects/skewed-emacs && ./compose-dev down`.
3. `cd /projects/basilisk && ./basilisk up`.
4. Verify: containers healthy, dashboard lists the right services, SLIME
   discovery intact, `mcp__skewed-emacs` answering again.

Step 2 stops the `skewed-emacs` container, which is the one carrying the
lisply endpoint an agent session talks through — so **whoever runs this
loses their MCP connection for the duration** and gets it back when the
stack returns (mcp-exec respawns; ~2 min end-to-end per
skewed-emacs/CLAUDE.md). That is an argument for Dave driving it from a
terminal rather than an agent driving it through the thing it is
restarting.

## RIP BLOCKER: `~/.claude.json` still points at `skewed-emacs/mcp/mcp-exec`

Found 2026-08-15, after the narad cutover, and it would have broken
silently rather than loudly.

Two MCP client configs exist on this box and only **one** of them is
regenerated by the merge:

| config | consumer | after the cutover |
|---|---|---|
| `mcp/claude_desktop_config.json` | Claude Desktop | **regenerated** — all six servers now say `/projects/basilisk/mcp/mcp-exec` |
| `~/.claude.json` | **Claude Code on the host** (agent sessions) | **stale** — all six still say `/projects/skewed-emacs/mcp/mcp-exec` |

It works today only because the yard was **copied, not moved**, so
`skewed-emacs/mcp/mcp-exec` still exists. Both files are byte-identical
copies of the same script.

**The moment the rip deletes `skewed-emacs/mcp/`, every Claude Code MCP
connection on this box stops resolving** — and not with a useful error at
the moment of deletion, but at the next session start, as six servers
that simply fail to launch.

The asymmetry is worth understanding rather than just patching: the merge
writes the Desktop config as *generated output*, so it followed the move
for free — Dave refreshed it straight out of `basilisk/mcp/` after the
cutover and every path in it was already correct. `~/.claude.json` is
hand-maintained state that nothing generates, so nothing updated it.

These really are two separate registries, which is exactly the trap: the
Desktop list's health says nothing about the host-side Claude Code one,
and vice versa. Verifying one looks like verifying both.

**Fix before the rip**, either by rewriting those six `args[0]` entries to
`/projects/basilisk/mcp/mcp-exec`, or — better, since it stops this
recurring — by teaching the merge step to own that file the way it owns
the Desktop config.

Worth checking the same question for any other machine in the fleet that
runs agent sessions against a stack, and for the container-side
`mcp-container.json` consumers.

## Suggested order

1. Land this manifest (done — you are reading it).
2. `git mv` the unambiguous yard, fix `skewed-gen-fittings-file`, sweep
   the 13 external references, regenerate all five stacks and **diff the
   generated compose YAML — it must come out byte-identical.** That diff
   is the acceptance test for the whole move; it is what proved the
   symbolic-roster conversion correct and it works just as well here.
3. Split `mcp/`, then `README.md` / `CLAUDE.md`.
4. Fix `bootstrap_from_image`, and rebuild the skewed-emacs image if
   option (1) is chosen.
5. Only then update the public site — pointing readers at a Basilisk with
   no repo of its own would be the wrong order.

## Deliberately not in scope

The stack's *behavior* does not change here. Symbolic rosters, crew
levels, the recommended-Captain rule, roster-derived heap probes and the
`eskew`/`egskew` messages all landed in `skewed-emacs` before this split
and move across as-is. If the compose YAML changes, something went wrong.

(The Captain rule was briefly stricter — the generator *refused* a roster
without `:captain`. Revised the same day: it warns and proceeds, because
a Pilot-only roster is a standalone Cyclops deployment and an
engineer-only roster is a standalone monolithic KBE server, and both are
shapes we ship. Only `skewed-network` is structurally required.)
